let a = 4
let b = 5
let c = 9

if (a > b && a > c) {
    console.log(`a (${a}) is the GOAT`)
} else if ( b > a && b > c ) {
    console.log(`b (${b}) is the GOAT`)
} else {
    console.log(`c (${c}) is the GOAT`)
}